# Data Management

## What is the goal

The goal of our data is to train our models the most efficiently possible.

## Where we get our data from

## What we do with data

Modify the data to have more artifacts on them. This point has to be studied as some factors (such as adding noise) might not be relevant when training our dataset.
If it gets bigger and the data generated by our alterations is not really relevant, it would be as we had a duplication of our data. Which is pointless.
